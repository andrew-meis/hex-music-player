import { Box } from '@mui/material';
import React from 'react';

const Footer = () => (
  <Box
    borderTop="1px solid"
    borderColor="border.main"
    height="10px"
    maxWidth={900}
    mx="auto"
    width="89%"
  />
);

export default Footer;
